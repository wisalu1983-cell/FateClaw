---
description: FateClaw 项目的 plan/session 系统约定。规定主计划、活跃会话总表、子计划与 Q-xx 问题索引的职责、路径和回写关系。
alwaysApply: true
---

# FateClaw Plan System

## 真源文件

对任一 `<项目>/MVP/Management/`，计划系统默认使用以下文件：

- `项目状态索引.md`：项目当前有效状态。
- `当前执行计划.md`：唯一主计划。
- `活跃会话总表.md`：当前仍活跃的 session 总览。
- `待解决问题清单.md`：项目级问题/决策索引，使用 `Q-xx` 标识。
- `开发过程日志.md`：时序变更记录。
- `开发过程问答.md`：稳定口径沉淀。
- `子计划/`：复杂分叉问题的独立子计划。

如果任务是多步、跨会话或需要多人/多窗口协同，而 `当前执行计划.md`、`活跃会话总表.md` 或 `子计划/` 尚不存在，应先创建最小骨架，再开始执行。

## 角色分工

- `当前执行计划.md`：唯一主计划，回答“当前主线怎么走”。
- `活跃会话总表.md`：记录当前项目下所有仍活跃的 session；只保留 `进行中`、`阻塞中`、`等待确认` 的项。
- `待解决问题清单.md`：记录项目级待决问题和已形成的结论；不要把普通 session 级 todo 直接写成 `Q-xx`。
- `子计划/*.md`：记录复杂阻塞、分叉调查或局部独立方案；必须挂靠父计划和父任务。

## 硬约束

- 一个项目任一时刻只能有一个主计划处于进行中。
- 一个 session 任一时刻只能领取一个任务或一个子计划。
- 开始新 session 前，必须先在 `活跃会话总表.md` 中登记或领取对应条目。
- session 关闭、交接或暂停前，必须先回写结果，再更新或移除 `活跃会话总表.md` 中的对应条目。
- `活跃会话总表.md` 不是历史归档；已关闭的 session 应在完成回写后移出该表。
- 新问题只有在成为项目级待决问题或需要长期引用时，才升级写入 `待解决问题清单.md`。
- 如果新增、删除、重命名 `当前执行计划.md`、`活跃会话总表.md`、`子计划/`，或这些文件的职责发生变化，必须同步更新 `<项目>/目录结构索引.md`。
- 本规则只定义 plan/session 系统；管理文档同步细则按 `fateclaw-background-doc.mdc` 执行。

## 活跃会话总表最小字段

`活跃会话总表.md` 的每个条目至少包含：

- `Session ID`
- `任务标题`
- `父计划`
- `父任务`
- `关联 Q-xx`
- `状态`
- `最后同步时间`
- `下一步`
- `阻塞说明`

## 子计划最小字段

每个 `子计划/*.md` 至少包含：

- `父计划`
- `父任务`
- `关联 Q-xx`
- `Session ID`
- `状态`
- `目标`
- `关闭条件`

## 标准工作流

1. 进入项目后，先读 `项目状态索引.md`、`当前执行计划.md`、`活跃会话总表.md`，必要时再读 `待解决问题清单.md` 与相关 `子计划/*.md`。
2. 领取任务时，在 `活跃会话总表.md` 中登记当前 session。
3. 执行过程中若出现新问题，先分类为“局部细节 / 复杂阻塞 / 范围变化”。
4. 复杂阻塞可新建 `子计划/*.md`；范围变化必须先回到 `当前执行计划.md` 做 replan。
5. 结束或交接时，把结果回写到 `当前执行计划.md`，并按需要同步 `开发过程日志.md`、`待解决问题清单.md`、`开发过程问答.md` 和 `项目状态索引.md`。
6. 回写完成后，再更新或移除 `活跃会话总表.md` 中的 session 条目。

## 冲突处理

- 如果两个 session 试图处理同一父任务或同一文件区域，先停止并显式标记冲突，不得默认并行推进。
- 如果子计划结论改变了主计划的目标、范围或验收标准，先回写主计划，再继续下游执行。
