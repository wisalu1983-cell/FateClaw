---
description: 本仓库的多项目文档结构与设计文档确认状态分级约定。在引用、创建、修改项目文档时自动生效。
alwaysApply: true
---

# 仓库文档结构

## 仓库概况

本仓库（FateClaw）包含多个游戏设计尝试方向，每个方向以独立子目录组织。

## 目录约定

| 路径 | 内容 |
|------|------|
| `Brainstorming/` | 跨项目的早期创意与头脑风暴材料，仅供回溯 |
| `FateAgent/` | Agent 驱动的圣杯战争模拟（已封存） |
| `TinyMB/` | Tiny Mount & Blade，当前活跃方向 |

## 项目内部标准结构

每个项目目录按以下结构组织（以 TinyMB 为准）：

```
<项目>/
├── MVP/
│   ├── Management/    项目管理（状态索引、日志、问题清单）
│   ├── Design/        面向开发的设计文档
│   └── Dev/           开发实现（代码、配置等）
└── 参考资料/           调研、对标分析、外部资料整理
```

管理文档的详细分工见 `fateclaw-background-doc.mdc`。

## 确认状态三级制

各项目设计文档统一使用三级确认制。每份文档的当前级别以该项目的状态索引文档为准：

| 级别 | 含义 | 使用原则 |
|------|------|---------|
| **A. 已确认基线** | 经过用户逐项讨论并明确确认 | 可直接作为后续工作的输入和依据 |
| **B. 参考稿** | 方向有依据，但细节未逐项确认 | 可参考，不可直接视为定稿 |
| **C. 未确认草案 / 已替代** | 助手推导生成未经确认，或已被新方案替代 | 不可作为开发基线，仅供素材参考 |

## 项目状态索引

每个项目的管理文档中有一份状态索引作为权威入口。进入某个项目的新对话应优先读取对应的状态索引以对齐上下文。

当前各项目的状态索引：

| 项目 | 状态索引文档 |
|------|-------------|
| TinyMB | `TinyMB/MVP/Management/项目状态索引.md` |
| FateAgent | `FateAgent/MVP/Management/MVP版本开发背景文档.md` |
