---
description: FateClaw 仓库的项目导航、标准目录与设计文档确认级别约定。在引用、创建、修改项目文档时自动生效。
alwaysApply: true
---

# 仓库文档导航

## 项目版图

- `Brainstorming/`：跨项目早期创意，仅供回溯。
- `TinyMB/`：当前活跃方向。
- `FateAgent/`：已封存方向。

## 标准结构

每个项目默认按以下结构组织：

```
<项目>/
├── MVP/
│   ├── Management/    状态索引、日志、问题清单、问答
│   ├── Design/        面向开发的设计文档
│   └── Dev/           开发实现
└── 参考资料/           调研与对标资料
```

管理文档的分工与同步规则见 `fateclaw-background-doc.mdc`。

## 根目录导航索引

每个活跃项目的根目录应维护一份导航文档，如 `TinyMB/目录结构索引.md`，用于说明当前有效目录结构、各目录职责和关键文档快速入口。

以下情况发生时，必须同步更新对应项目的 `目录结构索引.md`：

- 新增、删除、重命名项目根目录下的关键目录或关键文档。
- `MVP/Management/`、`MVP/Scope/`、`MVP/Design/`、`MVP/Dev/` 的职责分工发生变化。
- 新增、删除、重命名影响项目导航的关键管理文档，如 `当前执行计划.md`、`活跃会话总表.md`、`开发过程问答.md`、`子计划/`。
- “关键文档快速入口”中的推荐入口发生变化。

## 确认级别

每份设计文档的当前级别以对应项目的状态索引为准：

- `A. 已确认基线`：已逐项确认，可直接作为后续输入。
- `B. 参考稿`：方向有依据，但细节未逐项确认；可参考，不可视为定稿。
- `C. 未确认草案 / 已替代`：助手推导生成且未经确认，或已被新方案替代；仅供参考，不可作为开发基线。

## 状态索引入口

进入某个项目的新对话时，应优先读取其状态索引：

- `TinyMB`：`TinyMB/MVP/Management/项目状态索引.md`
- `FateAgent`：`FateAgent/MVP/Management/MVP版本开发背景文档.md`
