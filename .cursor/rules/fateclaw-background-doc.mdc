---
description: MVP 管理文档的角色、同步触发条件与更新顺序。任何会改变项目当前状态或稳定口径的工作，都必须按本规则同步管理文档。
alwaysApply: true
---

# 项目管理文档同步规则

## 管理文档角色

所有项目的管理文档统一位于 `<项目>/MVP/Management/`。

| 文档 | 作用 | 更新原则 |
|------|------|---------|
| `项目状态索引.md` | 当前有效状态的权威入口 | 只保留当前有效状态；旧结论移入 `开发过程日志.md` |
| `开发过程日志.md` | 按时间倒序记录里程碑、决策与关键变更 | 每次重要变更都记录时间、范围、结论、影响 |
| `待解决问题清单.md` | 全项目开放问题与任务追踪 | 新增、关闭、推迟、重述问题时同步更新 |
| `开发过程问答.md` | 沉淀解释、挑战和澄清后形成的稳定口径 | 同主题合并记录，只保留当前有效口径 |

## 硬约束

- 本目录下 `alwaysApply: true` 的规则属于硬约束，高于执行计划、待办列表和助手自行拆解的步骤。
- 即使计划未显式列出管理文档同步，也不能视为豁免；仍需补齐相关管理文档。
- 如果计划内容与本规则冲突，先按本规则执行，再在回复中说明补充同步了哪些管理文档。
- 即使管理文档已有未提交改动，也不能直接跳过；必须先读取当前内容，在不覆盖已有改动的前提下做最小增量更新。
- 如果用户要求“只改设计文档”，必须先明确说明这次仍将同步检查哪些管理文档，以及原因。
- 如果用户明确禁止同步其他文档，必须先提示会产生设计稿与管理文档之间的口径分裂风险，并等待用户再次确认。
- 如果用户在知情后仍坚持只改设计文档，可按其限制执行，但必须在回复中明确标注未同步的管理文档与对应风险。

## 同步触发条件

| 事件 | 状态索引 | 开发过程日志 | 待解决问题清单 | 开发过程问答 |
|------|---------|------------|---------------|-------------|
| 已确认设计决定被推翻或修改 | 必须 | 必须 | 按需 | 按需 |
| 新增设计文档 | 必须 | 必须 | 必须 | 按需 |
| 文档确认级别变化（升级 / 降级 / 替代） | 必须 | 必须 | 按需 | 按需 |
| 项目进入新阶段 | 必须 | 必须 | 按需 | 按需 |
| `Design/` 或 `Scope/` 下文档新增、重写、回标、替代、降级、升级 | 当前有效状态改变时必须 | 必须 | 必须 | 按需 |
| 开放问题被解决、推迟、重述，或新增开放问题 | 按需 | 按需 | 必须 | 按需 |
| 明确解释型追问、方案挑战与澄清 | 按需 | 按需 | 按需 | 仅复述旧结论时可不更新；新形成可复用口径时若已启用则必须 |

## 状态索引重点触发

以下情况发生时，必须同步检查 `项目状态索引.md`：

- 关键结论变更：已确认的设计决定被推翻或修改。
- 新文档产出：项目目录下新增了设计文档。
- 文档状态变化：某文档的确认层级发生变化，如升级、降级、替代。
- 讨论阶段推进：项目进入新阶段。

## 日志与问题清单重点触发

以下情况发生时，必须同步检查 `开发过程日志.md` 与 `待解决问题清单.md`：

- 设计基线变更：如 `Must/Later` 边界、首轮原型口径、阶段目标、已锁定决策发生变化。
- 设计文档实质修改：如 `Design/` 或 `Scope/` 下文档新增、重写、回标、替代、降级、升级。
- 开放问题状态变化：已有问题被解决、被推迟、被重述，或新增新的开放问题。

补充要求：

- `开发过程日志.md` 需记录本次变更的时间、范围、关键结论、影响面。
- `待解决问题清单.md` 需关闭已解决项、补充新问题，更新备注中的最新口径，并标注来源、依赖和状态。
- `项目状态索引.md` 只在“当前有效状态”改变时改写正文；被替代旧内容移入 `开发过程日志.md`，必要时可用简短注释标记变更点。
- `开发过程问答.md` 以主题合并记录，不按逐轮对话逐字转录；每条至少包含 `问题`、`澄清结论`、`影响`。若结论后续变化，正文改写为新口径，旧口径记入 `开发过程日志.md`。

## 问答沉淀判定

- 仅复述旧结论：如果只是把 `范围文档`、`状态索引`、现有设计稿中的既有口径重新解释给用户，没有新增边界、判断框架或统一说法，可不更新 `开发过程问答.md`。
- 新形成可复用口径：如果这轮问答新增了可复用的判断逻辑、概念边界、方案取舍标准或统一说法，即使源于追问，只要项目已启用 `开发过程问答.md` 就必须更新。
- 若用户明确要求“把这套判断逻辑沉淀成之后都可复用的口径”，默认按“新形成可复用口径”处理。

## 交互脚本

- 用户说“只改设计文档”时，先说明：本次仍需同步检查哪些管理文档，以及这些检查是为了避免状态索引、日志、问题清单或问答口径落后于设计稿。
- 用户说“不要同步其他文档”时，先提醒：只改设计稿会造成设计稿与管理文档之间的口径分裂风险；随后暂停执行，等待用户再次确认。
- 用户在知情后仍坚持不同步时，可按其限制执行，但在回复中必须再次注明：这次没有同步哪些管理文档，以及因此留下的风险。

推荐回复模板：

- 用户说“只改设计文档”时，优先使用类似表述：`这次除了设计稿本身，我还会同步检查 开发过程日志.md、待解决问题清单.md，并在必要时更新 项目状态索引.md / 开发过程问答.md，以避免口径分裂。`
- 用户说“不要同步其他文档”时，优先使用类似表述：`如果不同步 开发过程日志.md、待解决问题清单.md，以及必要时的 项目状态索引.md / 开发过程问答.md，就会出现设计稿与管理文档口径分裂。请确认你是否接受该风险，并仍要求我只改设计稿。`
- 用户明确禁止同步其他文档时，回复必须以“请确认你是否接受该风险并仍要求我只改设计稿”这类确认问句收尾；在收到二次确认前，不得承诺开始执行。

禁止行为：

- 在完成上述风险提示和二次确认前，不得直接承诺“我会只改该设计文档，不改其他文档”。
- 在完成上述风险提示和二次确认前，不得把“不同步其他文档”表述成默认合理或默认无风险。

## 状态索引膨胀控制

- 状态索引只保留当前有效状态。
- 结论被更新时，正文直接改写为新结论。
- 被替代的旧内容移入 `开发过程日志.md`。
- 必要时可用简短注释标记变更点，如 `> 注意：原方案为 X，已改为 Y。详见开发过程日志。`。

## 更新顺序

1. 确认本次会话命中了哪些同步触发条件。
2. 先更新 `开发过程日志.md`。
3. 再更新 `待解决问题清单.md`。
4. 若命中解释 / 澄清条件且属于“新形成可复用口径”，并且项目已启用，更新 `开发过程问答.md`。
5. 若当前有效状态被改变，最后改写 `项目状态索引.md`。
6. 回复用户时说明本次同步了哪些管理文档；若按用户确认只改设计稿，也要说明未同步项及风险。

## 完成前检查

在声称任何一次设计文档修改“已完成”前，必须显式检查：

- `开发过程日志.md` 是否已记录本次变更。
- `待解决问题清单.md` 是否已同步本次新增、解决、推迟或重述的问题。
- 若项目启用了 `开发过程问答.md`，本次解释 / 澄清型结论是否已沉淀。
- `项目状态索引.md` 是否仍然反映当前有效状态。

若任一项未同步，先补齐管理文档，再汇报完成。

## 当前各项目实际路径

| 项目 | 状态索引 | 开发过程日志 | 问题清单 | 开发过程问答 |
|------|---------|------------|---------|-------------|
| TinyMB | `TinyMB/MVP/Management/项目状态索引.md` | `TinyMB/MVP/Management/开发过程日志.md` | `TinyMB/MVP/Management/待解决问题清单.md` | `TinyMB/MVP/Management/开发过程问答.md` |
| FateAgent | `FateAgent/MVP/Management/MVP版本开发背景文档.md` | `FateAgent/MVP/Management/背景文档-变更日志.md` | `FateAgent/MVP/Management/待办事项清单.md` | 未启用 |

`FateAgent` 的文件命名沿用历史名称，功能角色与标准一致。
