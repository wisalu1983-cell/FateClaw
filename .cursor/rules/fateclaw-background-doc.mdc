---
description: 各 MVP 项目的管理文档结构标准与维护规范。在创建、修改项目管理文档时自动生效。
alwaysApply: true
---

# 项目管理文档结构与维护规范

## 标准目录结构

每个 MVP 项目的管理文档统一存放在 `<项目>/MVP/Management/`，包含三类文件：

```
<项目>/MVP/Management/
├── 项目状态索引.md       权威入口：项目背景 + 开发规划 + 当前进展 + 阶段性结论
├── 开发过程日志.md       变更记录：按时间倒序记录里程碑、决策变更、阶段推进
└── 待解决问题清单.md     开放问题追踪：汇总待办事项并标注状态和依赖
```

## 三类管理文档的分工

### 项目状态索引

- **定位**：项目当前状态的权威快照，新对话进入该项目时应优先读取
- **内容**：项目背景与定位、开发路线图、当前所处阶段、已完成工作、待确认决策、阶段性结论
- **更新原则**：只反映最新状态，被替代的旧内容移入开发过程日志

### 开发过程日志

- **定位**：项目的完整时序记录
- **内容**：每次重要变更、里程碑、决策的时间戳记录
- **格式**：按时间倒序排列，每条含日期、事件描述、关键发现/结论

### 待解决问题清单

- **定位**：全项目统一的任务追踪索引
- **内容**：各设计文档产生的待办事项，标注来源、依赖和状态
- **更新原则**：每完成一份设计文档后追加新产生的待办事项

## 何时必须更新状态索引

以下情况发生时，必须同步更新项目状态索引：

1. **关键结论变更**：已确认的设计决定被推翻或修改
2. **新文档产出**：项目目录下新增了设计文档
3. **文档状态变化**：某文档的确认层级发生变化（升级/降级/替代）
4. **讨论阶段推进**：项目进入新阶段

## 规则优先级与冲突处理

- 本目录下 `alwaysApply: true` 的规则属于**硬约束**，优先级高于用户附带的执行计划、待办列表和助手自行拆解的步骤
- 如果执行计划只列出了设计文档，但未显式列出管理文档同步，**不能视为豁免**；仍需按本规则补齐 `开发过程日志.md`、`待解决问题清单.md`，以及必要时的 `项目状态索引.md`
- 如果计划内容与本规则冲突，先按本规则执行，再在回复中向用户说明补充同步了哪些管理文档
- 即使管理文档本身已经存在未提交改动，也**不能直接跳过**；应先读取当前内容，在不覆盖已有改动的前提下做最小增量更新

## 何时必须同步开发过程日志与问题清单

以下情况发生时，除状态索引外，还必须同步检查并更新 `开发过程日志.md` 与 `待解决问题清单.md`：

1. **设计基线变更**：如 Must/Later 边界、首轮原型口径、阶段目标、已锁定决策发生变化
2. **设计文档实质修改**：如 `Design/` 或 `Scope/` 下文档新增、重写、回标、替代、降级、升级
3. **开放问题状态变化**：已有问题被解决、被推迟、被重述，或新增了新的开放问题

同步要求：

- `开发过程日志.md`：记录这次变更的时间、范围、关键结论、影响面
- `待解决问题清单.md`：关闭已解决项、补充新问题、更新备注中的最新口径
- `项目状态索引.md`：只在“当前有效状态”被改变时改写正文

## 膨胀控制

状态索引只保留**当前有效状态**：

- 结论被更新时，正文直接改写为新结论
- 被替代的旧内容移入开发过程日志
- 索引中用简短注释标记变更点（如 `> 注意：原方案为 X，已改为 Y。详见开发过程日志。`）

## 更新流程

1. 确认触发条件命中
2. 先检查这次变更是否同时触发 `开发过程日志.md` 与 `待解决问题清单.md` 的同步条件
3. 先在开发过程日志中记录本次变更
4. 再更新待解决问题清单中的状态、备注或新增问题
5. 最后更新状态索引正文
6. 更新完毕后提醒用户已同步更新了哪些管理文档

## 完成前检查

在声称“已完成”任何一次设计文档修改前，必须显式检查：

- `开发过程日志.md` 是否已记录本次变更
- `待解决问题清单.md` 是否已同步本次新增 / 解决 / 重述的问题
- `项目状态索引.md` 是否仍然反映当前有效状态

若任一项未同步，先补齐管理文档，再汇报完成。

## 当前各项目管理文档实际路径

| 项目 | 状态索引 | 开发过程日志 | 问题清单 |
|------|---------|------------|---------|
| TinyMB | `TinyMB/MVP/Management/项目状态索引.md` | `TinyMB/MVP/Management/开发过程日志.md` | `TinyMB/MVP/Management/待解决问题清单.md` |
| FateAgent | `FateAgent/MVP/Management/MVP版本开发背景文档.md` | `FateAgent/MVP/Management/背景文档-变更日志.md` | `FateAgent/MVP/Management/待办事项清单.md` |

> FateAgent 的文件命名沿用历史名称，功能角色与标准一致。
