---
description: 各 MVP 项目的管理文档结构标准与维护规范。在创建、修改项目管理文档时自动生效。
alwaysApply: true
---

# 项目管理文档结构与维护规范

## 标准目录结构

每个 MVP 项目的管理文档统一存放在 `<项目>/MVP/Management/`，包含三类文件：

```
<项目>/MVP/Management/
├── 项目状态索引.md       权威入口：项目背景 + 开发规划 + 当前进展 + 阶段性结论
├── 开发过程日志.md       变更记录：按时间倒序记录里程碑、决策变更、阶段推进
└── 待解决问题清单.md     开放问题追踪：汇总待办事项并标注状态和依赖
```

## 三类管理文档的分工

### 项目状态索引

- **定位**：项目当前状态的权威快照，新对话进入该项目时应优先读取
- **内容**：项目背景与定位、开发路线图、当前所处阶段、已完成工作、待确认决策、阶段性结论
- **更新原则**：只反映最新状态，被替代的旧内容移入开发过程日志

### 开发过程日志

- **定位**：项目的完整时序记录
- **内容**：每次重要变更、里程碑、决策的时间戳记录
- **格式**：按时间倒序排列，每条含日期、事件描述、关键发现/结论

### 待解决问题清单

- **定位**：全项目统一的任务追踪索引
- **内容**：各设计文档产生的待办事项，标注来源、依赖和状态
- **更新原则**：每完成一份设计文档后追加新产生的待办事项

## 何时必须更新状态索引

以下情况发生时，必须同步更新项目状态索引：

1. **关键结论变更**：已确认的设计决定被推翻或修改
2. **新文档产出**：项目目录下新增了设计文档
3. **文档状态变化**：某文档的确认层级发生变化（升级/降级/替代）
4. **讨论阶段推进**：项目进入新阶段

## 膨胀控制

状态索引只保留**当前有效状态**：

- 结论被更新时，正文直接改写为新结论
- 被替代的旧内容移入开发过程日志
- 索引中用简短注释标记变更点（如 `> 注意：原方案为 X，已改为 Y。详见开发过程日志。`）

## 更新流程

1. 确认触发条件命中
2. 先在开发过程日志中记录本次变更
3. 再更新状态索引正文
4. 更新完毕后提醒用户已同步更新

## 当前各项目管理文档实际路径

| 项目 | 状态索引 | 开发过程日志 | 问题清单 |
|------|---------|------------|---------|
| TinyMB | `TinyMB/MVP/Management/项目状态索引.md` | `TinyMB/MVP/Management/开发过程日志.md` | `TinyMB/MVP/Management/待解决问题清单.md` |
| FateAgent | `FateAgent/MVP/Management/MVP版本开发背景文档.md` | `FateAgent/MVP/Management/背景文档-变更日志.md` | `FateAgent/MVP/Management/待办事项清单.md` |

> FateAgent 的文件命名沿用历史名称，功能角色与标准一致。
