---
description: FateClaw MVP 版本开发背景文档的维护规范——何时更新、如何控制膨胀、如何通过变更日志留档历史决策。
alwaysApply: true
---

# MVP 背景文档维护规范

## 目标文档

- 主文档：`MVP/Doc/MVP版本开发背景文档.md`
- 变更日志：`MVP/Doc/背景文档-变更日志.md`

## 定位

主文档是项目当前状态的权威快照，不是完整讨论记录。它应当永远反映最新状态，让任何新对话窗口可以快速对齐上下文。

## 何时必须更新

以下情况发生时，必须同步更新主文档：

1. **关键结论变更**：已确认的设计决定被推翻或修改
2. **新文档产出**：`MVP/Doc/` 下新增了文档，需登记到产出物索引
3. **文档状态变化**：某文档的确认层级发生变化（升级/降级/替代）
4. **讨论阶段推进**：项目进入新阶段（如从"方向收敛"到"规则设计"）

## 膨胀控制

主文档只保留**当前有效状态**：

- 结论被更新时，正文直接改写为新结论
- 被替代的旧内容移入变更日志
- 主文档中用简短注释标记变更点（如 `> 注意：原方案为 X，已改为 Y。详见变更日志。`）

## 变更日志格式

`背景文档-变更日志.md` 按时间倒序记录，每条格式：

```
### YYYY-MM-DD: [变更摘要]

**变更内容**：[具体改了什么]
**变更原因**：[为什么改]
**影响范围**：[主文档中哪些章节被更新]
**旧内容摘要**：[被替代的旧结论要点]
```

## 更新流程

1. 确认触发条件命中
2. 先在变更日志中记录本次变更
3. 再更新主文档正文
4. 更新完毕后提醒用户已同步更新
