# TinyMB 项目状态索引

> **文档用途**：本项目的权威入口——项目背景、开发规划、当前进展和阶段性结论。
>
> **更新原则**：本文档只反映当前状态，历史变更记录在 `开发过程日志.md` 中。

---

## 一、项目背景

### 1.1 项目名称与定位

TinyMB（Tiny Mount & Blade）—— 骑马与砍杀极简版

TinyMB 是 **AIvilization Plus** 的 MVP 版本，以骑马与砍杀的核心玩法为基底，极简化后接入多 Agent 框架，用资源、关系和派系取舍来强化原始 AI 沙盒体验的目标感与博弈张力。

### 1.2 来龙去脉

#### 前序项目：FateClaw

FateClaw 最初计划做一个 Agent 驱动的圣杯战争模拟游戏（详见 `Brainstorming/Agent圣杯战争提案-对话提炼.md`）。MVP v1 阶段完成了 4 份 A 级设计文档（核心循环、数值框架、行动系统、情报系统），但复杂度严重膨胀，暂停并封存（详见 `FateAgent/MVP/Management/MVP-v1设计讨论封存总结.md`）。

#### 核心玩法选型

暂停后进行了系统化的核心玩法对标脑暴（详见 `Brainstorming/玩法对标与脑暴/02-核心玩法对标脑暴.md`），评估了 8 个候选方案，结论是 **K. 极简骑砍** 综合最强——骑砍本身就是"角色在持续运转的沙盒世界中闯荡"，与 AIvilization 架构同构，且是唯一在全部 10 种 Agent 能力模式上均达到高度利用、不依赖任何未成熟能力的方案。

#### TinyMB 的目标

基于上述结论，TinyMB 的目标是：

1. 将骑砍核心玩法极简化为可被 Agent 框架驱动的规则集
2. 基于该规则集开发可运行的 MVP
3. 验证核心体验假设和 Trumen 多 Agent 框架的技术可行性

### 1.3 技术底座

- **Trumen 多 Agent 框架**：Client / Agent Framework / Server(Runtime) 三层结构
- **Agent 框架职责**：驱动 Observe → Think → Decide → Act 循环
- **与玩法解耦**：框架通过标准化接口接收具体游戏的环境适配模型（身份/环境/行为/决策约束四类定义）
- **部署拓扑**：Agent Framework 按角色类型拆分部署——NPC Agent 在服务端运行，玩家 Agent 在用户侧运行（用户选择基模）。两类 Agent 对 Runtime 的接口相同：接收观察包、输出混合型结构化行动意图、由 Runtime 统一校验和结算
- **行为框架约束**：玩家 Agent 的行为框架由官方系统 prompt 锁定（不可修改），用户选择基模只影响推理质量
- **等价模型**：用户侧玩家 Agent = 游戏环境中的 OpenClaw 式 Agent Runtime（官方系统 prompt + 用户基模 + 游戏行动工具集）

详见 `Brainstorming/多Agent框架相关/多Agent框架讨论总结.md`

### 1.4 核心体验假设

我相信，喜欢 AI 沙盒与轻策略体验的玩家，在通过对话引导一个有自主意志的 Agent 于持续运转的城邦中闯荡时，会因为简化版《骑马与砍杀》规则带来的资源、关系与派系取舍，以及玩家意图与 Agent 办事能力（基模、agent skill、角色数值/传统能力、权限）共同塑造行动过程与结果，而明确感知不同基模与 Agent Skill 组合会在同等任务条件下产生不同的执行规划、路径、效率与结果，并愿意据此继续调整指令与迭代 Agent Skill。

**当前验证信号**：

- 玩家会主动多次查看 Agent 日志、NPC 动向和世界变化。
- 玩家会主动介入至少一次资源、关系或派系取舍，并愿意承担由此带来的后果。
- 玩家能复述至少一条由 NPC 自主行动和玩家介入共同产生的连锁事件，并表示想继续观察后续发展。
- 玩家能明确感知不同基模与 Agent Skill 组合在同等任务条件下会产生不同执行规划、路径、效率与结果，并据此调整指令、迭代 Agent Skill。

### 1.5 设计约束

| 约束 | 来源 |
|---|---|
| Agent 是一个"人"（有日常行为、情绪、日记） | AIvilization 架构 |
| 世界连续运行（非回合制） | AIvilization 架构 |
| 玩家引导而非控制 | AIvilization 架构 |
| 多 Agent 自主交互 | AIvilization 架构 |
| 放置/挂机体验 | AIvilization 架构 |
| 空间/地图维度 | AIvilization 架构 |
| Agent 内心可观察 | AIvilization 架构 |
| 依赖已成熟 Agent 能力，避开未成熟能力 | Agent 技术边界 |
| 开放式行动结果由玩家目标、基模、agent skill、角色状态与权限共同影响 | OpenClaw 式 Agent 设计 |
| agent skill 是任务能力包，不等同于传统数值技能树 | OpenClaw 式 Agent 设计 |
| 玩家 Agent 的四步循环效率与质量必须显著受基模和 agent skill 影响 | OpenClaw 式 Agent 设计 |
| 先回答"最小系统是什么" | MVP v1 复杂度膨胀教训 |

---

## 二、开发规划

### 2.1 总体路线

```
Phase 1: 极简规则集设计 ← 当前阶段
    ↓
Phase 2: MVP 技术方案设计
    ↓
Phase 3: MVP 开发实现
    ↓
Phase 4: 原型验证与迭代
```

### 2.2 Phase 1 详细规划（极简规则集设计）

| 步骤 | 内容 | 状态 |
|---|---|---|
| 1.1 骑砍核心循环调研 | 聚焦 6 个与 AIvilization 架构相关的维度，产出概要 | **已完成** |
| 1.2 概要评审 | 基于权威资料交叉验证，修正并补充遗漏 | **已完成** |
| 1.3 行动-聚落层级映射 | 建立骑砍行动与聚落层级的对应关系 | **已完成** |
| 1.4 极简化世界结构与地图 | 单城镇方案（母稿 6 区；首轮原型 4 区 + 3 派系） | **已完成** |
| 1.5 极简化核心循环与行为空间 | 连续时钟 + 首轮 4 区行动清单 + 维护压力 | **已完成** |
| 1.6 极简化数值骨架 | 4核心变量 + 随从系统 + 经济循环 + 繁荣度 | **已完成** |
| 1.7 极简化势力与 NPC 自主行为 | 首轮 6 名命名 NPC（母稿 12 名）+ 性格系统 + 派系博弈 + 事件驱动 | **已完成** |
| 1.8 极简化玩家引导与请示节奏 | 首轮最小请示基线 + 长期方针 + 日记系统（完整四级 Later） | **已完成** |
| 1.9 整合自审 | 风险评估 + 开放问题 + 自审检查清单（全部通过） | **已完成** |

---

## 三、当前进展

### 当前所处阶段

Phase 1（极简规则集设计）—— **设计初稿完成，体验假设与 MVP 范围文档 v0.7 已落地，5 份阶段 2 设计文档已完成首轮 `MVP-Must / Later` 回标；P1“时间结构与行动粒度”、P2“结构化行动意图格式”、P3“观察包与世界状态摘要字段”、P4“日志 / 请示包 / 状态回顾记录格式”已定稿；完整记忆系统机制转入 `Q-19` 后续明确任务，当前进入 P5 的无具体 skill 可玩基线范围定义；会话执行层已收缩为“主开发目录 + 短分支优先、`worktree` 按需启用”的单人开发约定，之后再做试玩**

### 当前所处的 game-mvp-workshop 阶段

| 阶段 | 状态 | 说明 |
|---|---|---|
| 阶段 0：概念结晶 | **已完成** | 体验假设已确认 |
| 阶段 1：范围界定 | **已确认** | `MVP/Scope/TinyMB MVP 范围文档.md` v0.7，MoSCoW 已审定，已补入基模与 agent skill 的必须验证点，并同步到 6 名命名 NPC / 4 个活跃区域 / P1 时间口径 / P2 行动契约 / P4 记录口径 |
| 阶段 2：精益设计 | **进行中** | 5 份设计文档已完成首轮 `MVP-Must / Later` 回标；P1、P2、P3、P4 已完成，当前推进 P5（无具体 skill 的可玩基线范围），并把完整记忆系统机制保留为 `Q-19` 后续任务；会话执行层已收缩为单人开发版分支 / `worktree` 约定：默认主开发目录 + 短分支，`worktree` 仅按需启用，之后进入首轮试玩 |
| 阶段 3~6 | **未开始** | 尚未进入实现规划、原型构建、测试和决策门 |

### 已完成的工作

1. **骑砍核心循环调研**（步骤 1.1）
   - 从 6 个维度（大地图/日常循环/势力与NPC/社交政治/经济/成长路径）完成了目标导向的调研
   - 产出嵌入 `MVP/Design/极简骑砍规则集.md` 第二章

2. **概要评审**（步骤 1.2）
   - 基于骑砍官方 Wiki 和 TaleWorlds 开发日志做了逐节交叉验证
   - 修正了聚落数量标注、领主性格分类等数据
   - 补充了四变量平衡框架、宴会系统、聚落繁荣度、任务系统等遗漏机制
   - 评审结论嵌入 `MVP/Design/极简骑砍规则集.md` 第 2.7 节

3. **行动-聚落层级映射**（步骤 1.3）
   - 梳理骑砍全部行动类型，映射到聚落层级
   - 关键发现：城堡无独占行动，村庄仅独占招兵
   - 详见 `参考资料/骑砍行动与聚落层级映射.md`

4. **世界结构方案锁定**（步骤 1.4）✅
   - 经过三轮讨论（方案 A/B/C → 行动映射修正 → 单城镇可行性验证），最终锁定**单城镇方案**
   - 核心机制验证确认：30 项骑砍核心机制中 29 项在单城镇下保留，详见 `参考资料/单城镇方案核心机制验证.md`
   - 设计方案：石冠城（母稿 6 区域 + 3 大派系；首轮原型启用 4 活跃区域），已写入 `MVP/Design/极简骑砍规则集.md` 第三章

5. **体验假设与 MVP 范围文档补齐**（workshop 阶段 0/1）
   - 已明确 TinyMB 当前要验证的核心体验，并写入本文档
   - 已新增并更新 `MVP/Scope/TinyMB MVP 范围文档.md` 至 v0.7，对现有设计进行了 MoSCoW 收边，并补入基模与 agent skill 的必须验证点，且已同步到当前首轮原型口径
   - 初步结论：现有 5 份设计文档属于阶段 2 的先行产出，需要按范围文档回标优先级，并收敛首版 agent skill 与最小能力调整机制

6. **阶段 2 设计文档首轮回标完成**（workshop 阶段 2）
   - 已对 `01-05` 五篇设计文档完成 `MVP-Must / Later` 回标
   - 首轮可验证原型口径已锁为：**4 活跃区域 / 6 名命名 NPC / 轻量外部压力 / 最小请示基线**
   - 当前主线已调整为：先完成 skill 前置任务与无具体 skill 的可玩基线，再基于试玩结果收敛首版 agent skill，并定稿最小能力调整机制

7. **P1 时间结构与行动粒度已定稿**
   - 已将“半天制 / 每天 2 行动”改为“连续时钟 + 10 分钟基本粒度 + 区域间 5~20 分钟移动耗时”
   - 压力来源改为：跨区折返成本、时段窗口、事件时效
   - 已同步更新 `02-核心循环与行为空间.md`、`01-世界结构与地图.md`、`05-玩家引导与请示节奏.md`、`MVP/Scope/TinyMB MVP 范围文档.md` 与 `MVP/Design/极简骑砍规则集.md`

8. **新增开发过程问答文档并固化跨 session 自动维护规则**
   - 已新增 `MVP/Management/开发过程问答.md`，用于沉淀开发过程中被明确追问、挑战、解释和澄清后形成稳定口径的问题
   - 已将历史主要澄清主题回填到该文档，包括 MVP 边界、Agent Skill 定义、时间结构、结构化行动意图与混合方案等
   - 已更新 `.cursor/rules/fateclaw-background-doc.mdc`，要求后续在 TinyMB 的任意会话中，只要出现形成稳定口径的解释 / 澄清型对话，就自动同步更新该文档

9. **初始化计划系统骨架与多 session 协同约定**
   - 已新增 `MVP/Management/当前执行计划.md`，作为 TinyMB 当前唯一主计划
   - 已新增 `MVP/Management/活跃会话总表.md`，用于统一登记当前仍活跃的全部 session
   - 已新增 `MVP/Management/子计划/README.md`，作为复杂阻塞与分叉问题的子计划目录骨架
   - 当前已明确：体系建立前存在的旧 session 不会被自动接管；若仍活跃，需先人工补登记后再继续执行

10. **P2 结构化行动意图格式已定稿**
   - 已正式采用“混合方案”：`Runtime` 只权威执行一个主动作，行动意图同时携带后续意图、中断点、能力依据 / 缺口与回退策略
   - 已明确四类主动作：`move / act / ask / wait`
   - 已明确：若当前不在目标区域，主动作必须先是 `move`；若已命中重要决策 / 高风险边界，主动作改为 `ask`
   - 已同步更新 `02-核心循环与行为空间.md`、`05-玩家引导与请示节奏.md`、`04-势力与NPC自主行为.md`、`MVP/Scope/TinyMB MVP 范围文档.md` 与 `MVP/Design/极简骑砍规则集.md`

11. **P3 观察包与世界状态摘要字段已定稿**
   - 已将首轮观察包收口为：`self_state / local_snapshot / world_state_summary / memory_recall / available_actions`
   - 已明确：`world_state_summary` 只放普通人普遍会知道的客观公共信息，并压缩为 `top_public_signals`（`1~3` 条）
   - 已明确：`memory_recall` 只承接当前问题触发的个人记忆 `Top N`；当前观察与被召回记忆共同作为 Agent 决策输入
   - 已明确：`available_actions` 由 `Runtime` 权威裁定，等价于当前状态下 UI 可点击的合法动作集合
   - 已新增 `Q-19`：完整记忆系统的写入、预算、衰减与覆盖机制留作后续明确任务

12. **P4 日志 / 请示包 / 能力依据展示格式已定稿**
   - 已采用“**底层 `decision_record` + 展示层 `log_card / ask_card / review_card`**”的双层结构
   - 已明确：玩家可读层默认采用**第一人称**；玩家 Agent 与命名 NPC 共用同一套核心记录 schema，只在展示权限和可见性上裁剪
   - 已明确：`decision_record` 的 `MVP-Must` 必填字段需要逐一解释作用，作为后续开发拆实现、做回放和做差异验证的稳定接口
   - 已明确：`review_card` 只聚合既有记录，不新增底层未出现的新事实

13. **补齐项目内 `worktree` 最小基础设施与工作目录约定（v1）**
   - 已新增仓库根目录 `.gitignore`，当前最小内容为：`/.worktrees/`
   - 已明确：`设计文档 / 管理文档 / 计划文档` 默认在主开发目录 `D:/01-工作/Garena/GI/FateClaw` 执行
   - 已明确：`代码实现 / 并行开发 / 高风险改动` 才进入项目内 `/.worktrees/`
   - 已明确：进入 `worktree` 的 session 在收尾前必须显式选择“继续留在 `worktree` / 回灌主开发目录 / 通过 git 集成”

14. **将执行约定收缩为单人开发版分支 / `worktree` 工作流（v2）**
   - 已明确：`main` 作为当前可继续推进的稳定基线
   - 已明确：普通任务默认在主开发目录从 `main` 新开短命任务分支，建议使用 `task/*`、`fix/*`、`spike/*`
   - 已明确：`设计文档 / 管理文档 / 计划文档` 的小修可直接在主开发目录处理并当天提交；多文件文档任务与普通代码任务仍优先使用短分支隔离
   - 已明确：`/.worktrees/` 继续保留，但仅在需要并行保留多个任务现场、插入热修或进行高风险实验时启用

### 阶段 2 当前主线（skill 相关开发顺序）

| 顺序 | 任务层 | 当前要求 | 说明 |
|---|---|---|---|
| 1 | **前置任务** | 先锁玩法与接口地基：时间结构、行动粒度、观察包、结构化行动意图、日志/请示字段；做无具体 skill 的可玩基线；固定基准模型做首轮试玩 | 这一步只为 skill 提供稳定挂载点和验证环境，不提前定死具体 skill 列表 |
| 2 | **具体 skill 设计** | 基于试玩结果筛选值得做成 skill 的高频行为、常见失败点与可感知差异，再收敛首版 skill 清单与作用边界 | 具体 skill 设计依赖真实玩法与测试结果，当前只保留候选方向 |
| 3 | **skill 接入与验证** | 在 Q-09 收敛后，再做预制库、槽位限制、配置切换、日志/请示展示，以及同模型有/无 skill 对照和跨模型对照 | skill 接入与验证晚于前置任务和具体 skill 设计，避免把实现顺序混在一起 |

### 阶段 2 下一步可执行前置任务

> 当前执行位置：**P1、P2、P3、P4 已完成**，下一步进入 **P5** 的无具体 skill 可玩基线范围定义。

| 顺序 | 任务 | 当前目标 | 主要产出 |
|---|---|---|---|
| P1（已完成） | **定稿时间结构与行动粒度** | 解决“半天制/每天 2 行动”带来的强回合感问题，明确短行为时长、跨区移动耗时、同一区域连续办事的基本节奏 | `02-核心循环与行为空间.md` 的时间系统定稿口径，以及总览文档的同步口径 |
| P2（已完成） | **定稿结构化行动意图格式** | 明确玩家 Agent 与 NPC Agent 向 Runtime 输出的最小行动意图结构，包括目标区域、行动类型、必要参数与中断/请示点 | 面向 Runtime 校验与结算的最小行动意图格式说明（混合方案） |
| P3（已完成） | **定稿观察包与世界状态摘要字段** | 明确 Runtime 向 Agent 提供哪些关键信息：位置、可见 NPC、事件、机会、资源、关系、全局局势、当前可执行行动，以及当前问题触发的个人记忆召回 | 观察包五块结构、`top_public_signals` 口径与 `memory_recall` 接口 |
| P4（已完成） | **定稿日志 / 请示包 / 能力依据展示格式** | 先在无具体 skill 状态下跑通可观察性，并预留未来展示 skill 配置、能力依据与能力缺口的位置 | 统一的 `decision_record` 记录底座，以及 `log_card / ask_card / review_card` 的映射规则 |
| P5 | **定义无具体 skill 的可玩基线范围** | 明确首个可玩版本在不接具体 skill 的情况下至少要跑通哪些行为、哪些 NPC、自主循环和请示节奏 | “无具体 skill 基线版本”范围说明，作为后续试玩样本来源 |
| P6 | **固定首轮基准模型与试玩记录方式** | 收住变量，先用固定基准模型观察无 skill 基线下的高频任务、常见失败点、白跑路径与可感知差异 | 基准模型选择口径与试玩记录模板 |
| P7 | **基于 P1-P6 进入首轮可玩基线开发/试玩** | 用首个可玩版本产出真实玩法样本，为 Q-09/Q-10 后续收敛提供依据 | 高频行为清单、失败样本、后续 skill 候选输入 |

### 待确认的决策

| 决策 | 当前状态 |
|---|---|
| 世界结构方案 | **已锁定**：单城镇方案（石冠城，母稿 6 区域 3 派系；首轮原型启用 4 活跃区域） |
| 首轮命名 NPC 口径 | **已锁定**：6 名 |
| 外部压力方案 | **已锁定**：轻量替代物（来访者 / 匪患传闻 / 商路异动 / 农庄求援 / 警戒状态变化） |
| 首轮时间结构与行动粒度 | **已锁定**：连续时钟 + 10 分钟基本粒度 + 区域间 5~20 分钟移动耗时 + 同区连续办事 + 每 3 天结算 |
| 首轮结构化行动意图契约 | **已锁定**：采用混合方案；`Runtime` 只执行一个主动作，后续意图 / 中断点 / 能力依据 / 缺口 / 回退用于请示、日志、回放和差异验证 |
| 首轮观察包口径 | **已锁定**：观察包采用 `self_state / local_snapshot / world_state_summary / memory_recall / available_actions` 五块；其中 `world_state_summary` 只放公共客观信息，`memory_recall` 承接个人记忆召回 |
| 玩家交互基线 | **已锁定**：默认自主 + 重要决策请示 + 未回复安全回退 |
| 首轮日志 / 请示 / 状态回顾口径 | **已锁定**：底层采用统一 `decision_record`；对外派生 `log_card / ask_card / review_card`；玩家可读层默认第一人称，玩家 Agent 与命名 NPC 共用核心 schema |
| 首版 agent skill 清单 | **待可玩基线后收敛**：当前只保留候选方向；需在前置任务完成并经过首轮试玩后，按高频行动、常见失败点与可感知差异定稿 |
| 最小能力调整机制 | **待 Q-09 后定稿**：当前先固定基准模型做主验证，并预留 skill 配置与能力依据展示接口；`P4` 已将日志 / 请示 / 状态回顾的展示挂点收口到统一记录底座，具体 skill 调整机制与接入细节在 skill 清单收敛后确认 |
| 首轮记忆系统机制 | **待 Q-19 定稿**：当前只锁“观察包如何消费记忆结果”；完整写入、预算、衰减与覆盖规则保留为后续明确任务 |

---

## 四、阶段性结论

### 结论 1：骑砍与 AIvilization 架构的同构性已验证

骑砍的核心循环（目标驱动的连续行动流、多领主自主行为、势力兴衰演化）与 AIvilization 架构的 7 项约束天然兼容。调研未发现结构性冲突。

### 结论 2：骑砍策略层的核心是四变量平衡

Money / Renown / Honor / Relationships 四个变量相互牵制，构成骑砍策略层的灵魂。极简化时数值骨架至少需要覆盖这四个维度。

### 结论 3：极简化方向已明确

MVP 不需要还原骑砍的完整规模（22~57 城镇），只需保留机制的功能分化逻辑。

### 结论 4：城堡可与城镇合并（行动-聚落映射发现）

通过系统梳理骑砍全部行动与聚落层级的对应关系，发现：
- **城镇独占 8 项行动**（交易、同伴、佣兵、工坊、竞技、战俘、行会任务、酒馆），是行动最丰富的聚落类型
- **城堡没有任何完全独占的行动**——城堡能做的一切，城镇内部的城堡区都能做。城堡的独特价值纯粹在军事/战略层面
- **村庄仅独占 1 个核心行动**（招募基础兵源），可不作为独立地图节点而作为城镇的附属属性

因此建议世界结构从"城镇/城堡/村庄"三类简化为"城镇 + 村庄/哨卡"两类，城堡功能合并入城镇。详见 `参考资料/骑砍行动与聚落层级映射.md`

### 结论 5：单城镇方案可行（核心机制验证）

对骑砍 30 项核心玩法机制逐条验证后确认：
- 19 项在单城镇中完整保留，10 项变形保留（核心逻辑不变，空间尺度缩小），仅 1 项丧失（城际套利，非核心）
- 限制的是玩家行为自由度（空间选择减少、角色身份受限、进阶天花板降低）和战略纵深（从广度转为深度），而非核心机制
- 最终采用单城镇方案：石冠城，母稿 6 个功能区域 + 3 大派系；首轮可验证原型启用 4 个活跃区域

详见 `参考资料/单城镇方案核心机制验证.md`
